{
  "name": "middleware-only-at-path",
  "version": "1.0.3",
  "description": "Only use Express middleware at specific path.",
  "main": "lib/index.js",
  "scripts": {
    "compile": "rm -rf lib; babel -d lib/ src/",
    "prepublish": "npm run compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "express",
    "middleware",
    "use",
    "path",
    "allow-methods"
  ],
  "author": "Hugo Josefson <hugo@josefson.org>",
  "repository": "hugojosefson/middleware-only-at-path",
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-plugin-add-module-exports": "^0.1.2",
    "babel-preset-es2015": "^6.5.0"
  },
  "dependencies": {
    "path-to-regexp": "^1.2.1"
  },
  "babel": {
    "presets": [
      "es2015"
    ],
    "plugins": [
      "add-module-exports"
    ]
  },
  "license": "MIT"
}
